import 'package:flutter/material.dart';

class SenderMsgBubble extends StatelessWidget {
  const SenderMsgBubble({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}