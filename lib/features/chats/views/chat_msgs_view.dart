import 'package:flutter/widgets.dart';

class ChatMsgsView extends StatelessWidget {
  const ChatMsgsView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}